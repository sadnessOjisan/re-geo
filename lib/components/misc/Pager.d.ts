import * as React from "react";
interface Props {
    type?: "disabled";
    children: React.ReactNode;
    className?: string;
}
declare const Pager: (props: Props) => JSX.Element;
export default Pager;

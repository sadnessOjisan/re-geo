import React from "react";
interface Props {
    className?: string;
    children: React.ReactNode;
    speed?: number;
}
declare const Rotator: (props: Props) => JSX.Element;
export default Rotator;
